# [T2 – 10/08/2026]

## Mục tiêu tuần này

Bắt đầu dự án FANet: tái hiện (reproduce) paper Feedback Attention Network, hiểu cơ chế feedback attention, làm nền để tìm research gap.

## Done

- [x] **Setup môi trường**: Anaconda Python 3.12.7, PyTorch 2.12.0 (CPU — máy không có CUDA), cài opencv-python, albumentations, scikit-learn, tqdm, pyyaml
- [x] **Chạy code train FANet** trên dataset sessile-main-Kvasir-SEG (196 ảnh: 156 train / 40 val), ảnh 256×256, batch 2, Adam lr 1e-4, loss DiceBCELoss (0.5 BCE + 0.5 Dice)
  - Training đến epoch 53: train loss 0.476, **val loss 0.505 (best)** — `logs/train_log.txt`
  - Tốc độ ~1.5–2 phút/epoch trên CPU → chưa chạy hết 500 epochs theo paper
  - Cơ chế feedback hoạt động đúng như paper: mask khởi tạo bằng Otsu, nén RLE, chỉ cập nhật mask khi val_loss cải thiện
- [x] **Đọc sơ paper**: FANet (TNNLS 2022, arXiv:2103.17235) và UACANet (ACM MM 2021) — `docs/`
- [x] Viết guide kiến trúc chi tiết của FANet — `docs/FANet_Complete_Guide.md`

## Findings quan trọng

| Hiện tượng | Bằng chứng | Hệ quả |
|-----------|-----------|--------|
| Model ~7.72M params, U-Net 4 tầng (3→32→64→128→256) | `scripts/report.py`, `logs/report_log.txt` | Nhẹ, chạy được CPU |
| Val loss giảm 0.770 → 0.505 trong 53 epochs | `logs/train_log.txt` | Quá trình học ổn định, feedback không gây dao động lớn |
| MixPool: gate cứng `fmask ∨ prev_mask`, giữ 50% feature gốc làm safety net | `src/fanet/models/blocks.py` | Điểm nghi vấn: binarize có mất thông tin? (→ Report 2) |

## Experiments

| ID | Giả thuyết | Config | Kết quả | Link log |
|----|-----------|--------|---------|----------|
| — | (Chưa có experiment chính thức tuần này — chỉ chạy baseline training) | 53 epochs, CPU | val loss 0.505 | `logs/train_log.txt` |

## Will Do (On going)

- [ ] Đọc kỹ paper UACANet, so sánh cách dùng prediction của stage trước
- [ ] Đào sâu kiến thức nền: U-Net, segmentation techniques, attention
- [ ] Xác định research gap của MixPool

## Any Stuck / Open Questions

- Máy không có GPU → training chậm (~2 phút/epoch). Cân nhắc dataset nhỏ cho experiment, GPU cloud sau.

## Đính kèm link chi tiết

- Paper FANet: `docs/2103.17235v3.pdf` | Paper UACANet: `docs/3474085.3475375.pdf`
- Guide kiến trúc: `docs/FANet_Complete_Guide.md`
- Training log: `logs/train_log.txt` | Checkpoint: `checkpoints/checkpoint.pth`
